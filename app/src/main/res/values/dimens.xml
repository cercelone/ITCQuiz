<resources>

    <!-- padding for the quizz -->
    <dimen name="appPaddings">16dp</dimen>

    <!-- distance between radioButtons/checkBoxes and text of the answers -->
    <dimen name="answerLeftPadding">16dp</dimen>

    <!-- textSize for the answers -->
    <dimen name="answerTextSize">18sp</dimen>

    <!-- textSize for the questions -->
    <dimen name="questionsTextSize">20dp</dimen>

    <!-- padding for the questions -->
    <dimen name="questionPadding">16dp</dimen>

    <!-- distance between questions and answers -->
    <dimen name="questionMarginBottom">8dp</dimen>

    <!-- radioGroup left and right padding -->
    <dimen name="radioGroupPadding">16dp</dimen>

    <!-- checkBoxes left and right margin -->
    <dimen name="checkBoxMargin">16dp</dimen>

    <!-- finishButton right margin -->
    <dimen name="finishButtonMargin">16dp</dimen>
    <dimen name="relativePadding">8dp</dimen>

    <!-- checkBoxes left and right margin -->
    <dimen name="cardPaddingBottom">24dp</dimen>
    <dimen name="cardElevation">8dp</dimen>
    <dimen name="cardRadius">4dp</dimen>

    <dimen name="dividerHeight">1dp</dimen>

    <!-- elements of the SplashScreen -->
    <dimen name="editTextPadding">20dp</dimen>
    <dimen name="editTextMargin">20dp</dimen>
    <dimen name="startButtonPadding">32dp</dimen>
    <dimen name="layoutPaddingTop">48dp</dimen>
    <dimen name="layoutPaddingLeftRight">16dp</dimen>

</resources>
